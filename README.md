# python

list of data types:

1. Numbers (integer, float)
2. string (words)
3. boolean (true/false, yes/no)
4. list
5. dict (dictionary, hashmaps)
6. tuple
7. sets

mutiable datatypes: list, dict  # elemnts can be replaces or added in this datatype.
immutable datatypes: tuple, sets

there are many variable some of the know variables are

addition, subtraction, multiplication and division. power, squareroot 



#print (dir(server_list))
'''
'append', 'clear', 'copy', 'count', 'extend', 'index', 'insert', 'pop', 'remove', 'reverse', 'sort' -useful operations

'''

append = adding extra element to the list of elements
clear = is to remove the elements from the variable 
copy = Creates a new object but inserts references into it to the objects found in the original.
count = to identify the element in the list, how many types it appeared. 
extend = is to add the list of elements to the existing list as an individual elements
index - is to find the index of the element. As the python search from index start to end, even similar elements present in the list. it will show the 1st identified element index.
 case 1: in case similar value is available to find out we have to add the 1st index no. + 1 to the value.
insert = to add the element in to the particualr index value
remove =  is to remove the element from the list.
reverse = to reverse the list from end to start
sort = to arrange in order will change the variable 
sorted = to make a new list. 




